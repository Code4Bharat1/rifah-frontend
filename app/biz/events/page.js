import { BizEvents } from "@modules/workspace/components/biz-events";

export const metadata = {
  title: "Events & Trade Meets | Business Workspace | RIFAH Connect",
  description: "Browse upcoming chamber meets, reserve seats, and manage event passes in the business workspace.",
};

export default function EventsPage() {
  return <BizEvents />;
}
